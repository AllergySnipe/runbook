# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
*.egg-info/

# Secrets / local config
.env
.env.*
!.env.example

# Frontend (added Week 2)
web/node_modules/
web/dist/

# OS
.DS_Store
.neon

# Vendored agent skills (installed via `npx skills add`; not part of the app)
.agents/
.claude/
skills-lock.json

# Ingest cache — fetched corpus, re-downloadable
data/raw/

# Eval run artifacts (`runbook eval --json`); the committed baseline is evals/baseline.json
eval-results/

# Red-team run artifacts (`runbook redteam --json`)
redteam-results/
